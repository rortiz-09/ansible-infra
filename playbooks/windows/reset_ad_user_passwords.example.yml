---
# Example variables for playbooks/windows/reset_ad_user_passwords.yml.
# Keep real files with generated passwords out of Git and run with ANSIBLE_LOG_PATH=/dev/null.

ad_password_reset_show_passwords: true
ad_password_reset_unlock: true
ad_password_reset_change_at_logon: false
ad_password_reset_length: 14

ad_password_reset_users:
  - jviteri
  - kpin
  - cguzman
